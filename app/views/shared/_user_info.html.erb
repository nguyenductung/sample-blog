<% user ||= current_user %>
<%= link_to gravatar_for(user, size: 64), user %>
<h1>
  <%= user.name %>
</h1>
<span>
  <%= link_to "View profile", user %>
</span>
<span>
  <%= pluralize(user.entries.count, "entry") %>
</span>